interface:
  display_name: "Codex Attention Notifications"
  short_description: "Clickable alert when Codex needs your input"
  default_prompt: "Use $notify-codex-attention before asking me for a choice, input, or a manual UI action."
policy:
  allow_implicit_invocation: false
