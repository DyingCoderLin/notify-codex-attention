interface:
  display_name: "Codex Attention Notifications"
  short_description: "Clickable alert when Codex needs your input"
  default_prompt: "Use $notify-codex-attention to maintain event-driven alerts or notify me of a required manual action without a matching hook."
policy:
  allow_implicit_invocation: false
